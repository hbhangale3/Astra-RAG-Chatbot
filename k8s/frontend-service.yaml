apiVersion: v1
kind: Service
metadata:
  name: astra-frontend-service
  namespace: astra-rag
spec:
  type: NodePort
  selector:
    app: astra-frontend
  ports:
    - name: http
      port: 8501
      targetPort: 8501
      nodePort: 30085
